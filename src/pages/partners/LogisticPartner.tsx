import React from 'react';
import LogisticPartnerPage from '@/components/logisticpartner/LogisticPartnerPage';

const LogisticPartner: React.FC = () => {
  return (
    <main className="pt-0">
      <LogisticPartnerPage />
    </main>
  );
};

export default LogisticPartner;
